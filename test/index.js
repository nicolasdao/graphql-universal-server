const { assert } = require('chai')

describe('main', () => 
	describe('#do something', () => {
		it('Should do something well.', () => {
			assert.equal('1', '1')
		})
		it('Should do something else well.', () => {
			assert.equal('2', '2')
		})}))
